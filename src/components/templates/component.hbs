import { FunctionComponent, ReactElement } from 'react';

interface IProps {

}

const {{ name }}: FunctionComponent<IProps> = (props): ReactElement => {

  return (
    <div>This is the {{ name }} block</div>
  );
};

export default {{ name }};